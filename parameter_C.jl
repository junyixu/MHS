# 3D 旋转椭圆仿星器（论文 V.F：ε=0.33, κ=1.2, n_fp=3, q*=1.57, κ̄=1.0）
# 论文用 (n_r,n_θ,n_ζ)=(12,12,6)、p=3、2.5×10⁴ 步，最终力残差 7.4e-6。
# 这里分辨率对齐、步数按算力预算取；ι 剖面收敛远快于力残差。
n      = 12     # 极向/径向分辨率 (n_r = n_θ = n)
p      = 3      # 样条阶数
q_star = 1.57   # 环向场强参数
dt0    = 1e-2   # 初始时间步（:explicit_ls 下仅作起始值）
N_iter = 4000   # 弛豫迭代数
geometry = :stellarator
aspect   = 0.33  # ε 反环径比
elong    = 1.2   # κ（进入 ν(ζ) = 1 + (1−κ)cos(2 n_fp π ζ)）
nfp      = 3     # 环向场周期数
nzeta    = 6     # 环向单元数（必须 > 1，否则退化为轴对称）
kbar     = 1.0   # Solov'ev 初值的 κ̄（论文 V.F 取 1.0）
